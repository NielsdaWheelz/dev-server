# separated gateways await release and cutover qualification

problem: original skid's first separated release and live skid namespace
handback remain pending. herdr-mobile `v0.9.0` is published and its converted
host pin is present. github name transfer is complete.

impact: source preparation cannot establish live coexistence or authorize
activation of unfinished releases. provider homes also require fresh login
and hook trust; jarvis workers still need their prepared map activated after
pending incompatible actions are settled.

evidence: baseline dev-server `8498933` pins herdr-backed skid v0.8.0;
`skid-v1` source `927c55412eec7fa129a3325fb8f5ebf8051b6ea0` has the original
tmux host schema. herdr-mobile's release source is
`68a652d7ccbeaaf472ef1c5f3a4ea6949808bca4`. jarvis `bada736` in draft pr 42
provides the matching private worker homes; local checks passed, hosted checks
are blocked by its account billing limit, and deployment remains pending.
original skid's handoff supplies exact config/provider templates, a helper pin
qualified without auth on macbook, and the implemented ten-file fleet verifier.
live provider control is still pending. no live service,
phone, credential or tmux mutation is part of this source task.

owners and blockers:

- original app owner: published exact version/source commit/archive digests
  from repository id `1386409483` and original helper live compatibility.
- jarvis owner: activation of the worker/default map in the
  [runbook](../gateway-separation-runbook.md#provider-and-jarvis-contract),
  quiescence and cognition continuity evidence.
- root operator: published repository identity check, private recovery inventory, fresh
  provider setup, exact legacy-hook cleanup, stopped herdr snapshot reset,
  host namespace handback and native-platform coexistence qualification.
- phone/app owners: new-port reachability, both apps' profile journeys and
  wrong-product authentication rejection.

resolved when: real pins are committed, the original helper and both host
interfaces are qualified, and every live acceptance row in the runbook has
content-free passing evidence for macbook, devbox and arch. unavailable or
unexecuted boundaries remain `NOT_RUN`; syntax and stub probes do not close it.
