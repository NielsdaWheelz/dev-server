---
- name: Configure SSH daemon hardening
  ansible.builtin.copy:
    dest: /etc/ssh/sshd_config.d/99-devbox.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      PubkeyAuthentication yes
      PermitRootLogin no
      X11Forwarding no
      AllowUsers {{ devbox_user }}
  notify: Validate and restart ssh

- name: Ensure UFW denies incoming by default
  ansible.builtin.command: ufw default deny incoming
  changed_when: false

- name: Ensure UFW allows outgoing by default
  ansible.builtin.command: ufw default allow outgoing
  changed_when: false

- name: Allow Tailscale interface ingress
  ansible.builtin.command: ufw allow in on tailscale0
  changed_when: "'Skipping' not in devbox_ufw_tailscale.stdout"
  register: devbox_ufw_tailscale

- name: Remove public SSH fallback rule
  ansible.builtin.command: ufw --force delete allow 22/tcp
  register: devbox_ufw_delete_ssh
  changed_when: "'Rule deleted' in devbox_ufw_delete_ssh.stdout"
  failed_when: false

- name: Remove source-restricted bootstrap SSH rule
  ansible.builtin.command: "ufw --force delete allow from {{ devbox_bootstrap_ssh_cidr }} to any port 22 proto tcp"
  register: devbox_ufw_delete_bootstrap_ssh
  changed_when: "'Rule deleted' in devbox_ufw_delete_bootstrap_ssh.stdout"
  failed_when: false
  when: devbox_bootstrap_ssh_cidr | length > 0

- name: Allow public HTTP when enabled
  ansible.builtin.command: ufw allow 80/tcp
  register: devbox_ufw_allow_http
  changed_when: "'Skipping' not in devbox_ufw_allow_http.stdout"
  when: devbox_public_web

- name: Allow public HTTPS when enabled
  ansible.builtin.command: ufw allow 443/tcp
  register: devbox_ufw_allow_https
  changed_when: "'Skipping' not in devbox_ufw_allow_https.stdout"
  when: devbox_public_web

- name: Remove public HTTP when disabled
  ansible.builtin.command: ufw --force delete allow 80/tcp
  register: devbox_ufw_delete_http
  changed_when: "'Rule deleted' in devbox_ufw_delete_http.stdout"
  failed_when: false
  when: not devbox_public_web

- name: Remove public HTTPS when disabled
  ansible.builtin.command: ufw --force delete allow 443/tcp
  register: devbox_ufw_delete_https
  changed_when: "'Rule deleted' in devbox_ufw_delete_https.stdout"
  failed_when: false
  when: not devbox_public_web

- name: Enable UFW
  ansible.builtin.command: ufw --force enable
  changed_when: false

- name: Keep passwordless sudo for the single-user dev box
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/90-devbox-{{ devbox_user }}"
    owner: root
    group: root
    mode: "0440"
    validate: "visudo -cf %s"
    content: "{{ devbox_user }} ALL=(ALL) NOPASSWD:ALL\n"
