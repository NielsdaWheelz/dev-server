---
- name: Install managed dotfiles
  ansible.builtin.shell: |
    set -euo pipefail
    source "/home/{{ devbox_user }}/.local/share/dev-server/lib/common.sh"
    source "/home/{{ devbox_user }}/.local/share/dev-server/lib/dotfiles.sh"
    dotfiles_install
  args:
    executable: /bin/bash
  become: true
  become_user: "{{ devbox_user }}"
  environment:
    DEV_SERVER_HOME: "/home/{{ devbox_user }}"
    DEV_SERVER_ASSETS_DIR: "/home/{{ devbox_user }}/.local/share/dev-server/assets"
  register: devbox_dotfiles_install
  changed_when: false

- name: Set user shell to zsh
  ansible.builtin.user:
    name: "{{ devbox_user }}"
    shell: /usr/bin/zsh
